export class Note {
    noteId;
    header;
    description;
    dateCreate;
    constructor(noteId, header, description, dateCreate) {
        this.noteId = noteId;
        this.header = header;
        this.description = description;
        this.dateCreate = dateCreate;
    }
}
//# sourceMappingURL=note.js.map